# Frontend

This folder contains the React + Vite frontend for the task-tracker application.

For project setup and run instructions, use the repository root README at `../README.md` or the Portuguese version at `../README.pt-BR.md`.
